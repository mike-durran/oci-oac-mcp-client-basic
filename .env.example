# =============================================================================
# MCP Server Configuration
# =============================================================================
# URL to your MCP server endpoint (e.g., Oracle Analytics Cloud)
MCP_SERVER_URL=https://<oac-instance>.analytics.ocp.oraclecloud.com/api/mcp

# Bearer token for MCP server authentication - get from Oracle Analytics user profile
MCP_ACCESS_TOKEN=your-bearer-token-here

# =============================================================================
# OCI Generative AI Configuration
# =============================================================================
# OCID of the compartment where GenAI is enabled
OCI_COMPARTMENT_ID=ocid1.compartment.oc1..aaaa...

# OCID of the GenAI model to use (e.g., Cohere Command R+)
OCI_GENAI_MODEL_ID=ocid1.generativeaimodel.oc1..aaaa...

# OCI GenAI service endpoint (region-specific)
OCI_GENAI_ENDPOINT=https://inference.generativeai.us-ashburn-1.oci.oraclecloud.com

# Path to OCI configuration file
OCI_CONFIG_FILE=~/.oci/config

# Profile name in OCI config file
OCI_CONFIG_PROFILE=DEFAULT

# Authentication type: 'security_token' or 'api_key'
# Use 'security_token' after running: oci session authenticate
OCI_AUTH_TYPE=security_token
